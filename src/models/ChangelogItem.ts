export class ChangelogItem {
    public title: string;
    public subtitle: string;
    public description: string;

    public constructor(title: string, subtitle: string, description: string) {
        this.title = title;
        this.subtitle = subtitle;
        this.description = description;
        }
    }
